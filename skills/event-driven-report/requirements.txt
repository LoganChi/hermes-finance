# ===== event-driven-report · 依赖 =====
# 分两层：必需（--today 启动瞬间触发）/ 可选（特定模式才 import）

# ----- 必需（pip install -r requirements.txt）-----
pyyaml>=6.0
pydantic>=2.0
requests>=2.28
pandas>=2.0
numpy>=1.24
akshare>=1.10        # 注意: src/auction/fetcher.py:55 是模块级 eager import，不是可选

# ----- 可选（取消注释按需安装）-----
# openai>=1.0       # 真实 LLM 抽取（DeepSeek 兼容）。未装/无 key 会降级 mock，结果仅 demo
# tushare>=1.2      # 行情/news 后端切到 tushare 时（需 TUSHARE_TOKEN + 付费权限）
# baostock>=0.8     # 长历史分钟K回测时（pipeline 不触发）
